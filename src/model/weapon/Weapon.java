package model.weapon;

/**
 * Represents weapons that can be equipped by the player. ARROW represents a crooked arrow that can
 * travel in a tunnel and leave it other exit, and in a cave travels in a straight line.
 */
public enum Weapon {
  ARROW;
}
